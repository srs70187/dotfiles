options(scipen=999) #no scientific notation
options(geonamesUsername="srs70187") #geonames api login
# options(java.parameters="-Xmx2g") not sure whether this is necessary but will uncomment if slow
options("max.print"=50000)
#print full df width
options(tibble.width=Inf)
gcs_pid <- "stack-badges"
